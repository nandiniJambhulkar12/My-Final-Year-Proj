services:
  - type: web
    name: backend
    env: python
    branch: main
    root: Backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
